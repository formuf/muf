&НаСервере
Процедура ЭкспортНаСервере(ИмяФайла)

Код = Метаданные.Справочники.Товар.СтандартныеРеквизиты.Код.Представление(); // 1
Наименование = Метаданные.Справочники.Товар.СтандартныеРеквизиты.Наименование.Представление(); // 2
Количество = Метаданные.Справочники.Товар.Реквизиты.Количество.Представление(); // 3
Текст = новый ЗаписьТекста(ИмяФайла);
Выборка = Справочники.Товар.Выбрать();
Текст.ЗаписатьСтроку("Код"+";"+ "Наименование"+";"+ "Количество");
Пока Выборка.Следующий() Цикл
Текст.ЗаписатьСтроку (Выборка.Код + ";" + Выборка.Наименование +";"+ Выборка.Количество);
КонецЦикла;
Сообщить("Экспорт выполнен");
Текст.Закрыть();

КонецПроцедуры

&НаКлиенте
Процедура Ээкспорт(Команда)
ДиалогВыбора = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);

ДиалогВыбора.Заголовок = "Выберите файл";

Если ДиалогВыбора.Выбрать() Тогда

ИмяФайла = ДиалогВыбора.ПолноеИмяФайла;
КонецЕсли;
ЭкспортНаСервере(ИмяФайла);

КонецПроцедуры